import { Typography } from '@mui/material';
import { Box } from '@mui/system';

const Stats = () => {
  return (
    <Box
      sx={{
        mt: '1rem',
      }}
    >
      <Typography variant="body1">Here goes some statistics</Typography>
    </Box>
  );
};

export default Stats;
